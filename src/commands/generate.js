export default function (files = []) {
  // Generate Tests here
  // Extract shit using source.body. IDGAF about the type or sourceType
  console.dir(files[0]);
  console.log('File: %s Overwrite(Y/N): %s', files);
}
